'use client'
import { useState } from 'react'
import { ArrowUpRight, Check, Heart, Package, ShieldCheck, Wallet } from 'lucide-react'
import { AidShell, Footer, PageIntro } from '@/components/aid-shell'
export default function DonationsPage(){const [amount,setAmount]=useState('1000');const [sent,setSent]=useState(false);return <AidShell><PageIntro eyebrow="DIRECT RELIEF" title="Give where it matters" text="Every contribution is tracked from donation to delivery, with updates from verified partners."/><section className="route-content two-column"><article className="impact-card donation-box"><div className="directory-icon"><Heart size={22}/></div><h2>Support emergency response</h2><p className="muted">Choose an amount to fund meals, water, and medical kits in active zones.</p><div className="amount-grid">{['500','1000','2500','5000'].map(v=><button className={amount===v?'filter-active':''} onClick={()=>setAmount(v)} key={v}>₹{v}</button>)}</div><label className="form-stack">Custom amount<input value={amount} onChange={e=>setAmount(e.target.value)} inputMode="numeric"/></label><button className="primary full" onClick={()=>setSent(true)}>{sent?<><Check size={15}/> Donation recorded</>:<>Donate ₹{amount} <ArrowUpRight size={15}/></>}</button></article><article className="impact-card"><p className="eyebrow">TRANSPARENCY</p><h2>Where your money goes</h2><div className="impact-list"><span><Package size={15}/> Meals and groceries <b>48%</b></span><span><Wallet size={15}/> Water distribution <b>32%</b></span><span><ShieldCheck size={15}/> Medical kits <b>20%</b></span></div><div className="impact-progress"><i style={{width:'62%'}}/></div><p className="muted">₹5.2L delivered so far from 486 donors.</p></article></section><Footer/></AidShell>}
